export default class EpicPenny {
    constructor(img, title, description, votes) {
        this.img = img;
        this.title = title;
        this.description = description;
        this.votes = votes;
    }
}