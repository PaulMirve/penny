export default class EpicPenny {
    constructor(photo, title, description, votes) {
        this.photo = photo;
        this.title = title;
        this.description = description;
        this.votes = votes;
    }
}